#include "house.h"

int main() {
	// Notice: The walls will be constructed first,
	// and THEN the house. Why? Well, you can't have
	// a house without walls :) so everything is
	// default-constructed "inside out"
	house my_house;
}
