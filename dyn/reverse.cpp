#include <iostream>

using namespace std;

// Problem: Ask the user for an integer N. Then, as the user for N integers.
// Store them all in an array. Print them in backwards order.

// Fill in the TODO items to solve the problem.

// Get N from the user and return it. No error handling required here.
int get_n() {
	// TODO
}



int main() {
	// Ask the user for integer N
	get_n();

	// TODO (Write and call a function for this part). Create an array of N integers.
	// Hint: N is NOT a compile-time constant!

	// TODO (Write and call a function for this part). Fill the array with elements
	// received by the user. Hint: Arrays are pointers (dynamic arrays are literally
	// handled with pointers). If you pass that array into a function and modify the contents,
	// what will it do to the argument array?
	
	// TODO (Write and call a function for this part). Print the elements backward

	// TODO What do you need to remember to do with your array before the pointer
	// falls out of scope?
}
