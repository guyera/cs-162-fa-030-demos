// Task: Ask the user for a file name, then write "Hello, world!" to that file.

#include <iostream>

// TODO What do you need to #include in order to use ifstreams / ofstreams?

using namespace std;

int main() {
	string filename;
	// TODO (write and call a function) ask the user for the name of the file
	// they want us to write to. Store the filename in `filename`
	
	// Create the ofstream
	ofstream my_ofstream;

	// TODO Tell the ofstream to open the file whose name is stored in `filename`
	
	// TODO Write "Hello, world!" to the file. This is exactly like printing
	// "Hello, world!" to the terminal---we just use my_ofstream instead of cout.
}
